class MyModel{
  final String? title, image, price;
   MyModel(
    {  this.title,
     this.image,
    this.price, }
);}
class Lesson {
  final String? title,content,price,star;

  Lesson(
      {this.title, this.content, this.price, this.star});
}